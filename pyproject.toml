[project]
name = "chat-bot"
description = "generic chat-bot"
version = "0.1.0"

requires-python = ">=3.12"
dependencies = [
    "langgraph", 
    "langchain",
    "langchain-community", 
    "langchainhub", 
    "langchain-core",
    "langchain-ollama",
    "httpx",
    "jwcrypto>=1.5.6",
    "starlette>=0.46.1",
    "pyjwt>=2.10.1"
]